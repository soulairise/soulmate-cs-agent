# 인계 문서 — 새 컴퓨터에서 이어서 하기

**마지막 갱신** 2026-09-17 · **저장소** https://github.com/soulairise/soulmate-cs-agent (비공개)

소울매트 고객응대 에이전트. AIFFEL AI 에이전트 1기 「고객 응대 에이전트」 노드 실습작을
소울매트의 실제 업무로 다시 설계한 것.

---

## 1. 새 컴퓨터에서 처음 할 일

```bash
gh repo clone soulairise/soulmate-cs-agent
cd soulmate-cs-agent
./setup.sh
```

`setup.sh` 가 파이썬·가상환경·패키지·키 자리·데이터·안전규칙을 점검한다.
**키는 넣어 주지 않는다.** 없으면 넣는 명령을 화면에 출력하니 거기에 붙여넣으면 된다.

`gh` 가 없으면 `brew install gh && gh auth login` 먼저. 비공개 저장소라 로그인이 필요하다.

### 필요한 키 두 개

| 무엇 | 어디에 | 없으면 |
|---|---|---|
| `OPENAI_API_KEY` | `~/.config/harness-lab/env` | 에이전트가 아예 안 돈다 |
| `SM_WEB_CODE` | `~/.config/soulmate-cs-agent/env` | `setup.sh` 가 새로 만들어 준다 |

두 파일 모두 권한 600. **저장소에 넣지 말 것.**

### 쓰는 법

```bash
./run.sh                   # 터미널에서 대화
./run.sh web               # 브라우저 → http://localhost:8848
./run.sh eval --repeat 3   # 네 가지 지표 측정
./run.sh eval --only e2e   # 안전 경로만 (빠름)
```

---

## 2. 지금 어디까지 왔나

| 지표 | 값 | 비고 |
|---|---|---|
| ① 의도 분류 | 0.955 · macro F1 0.953 | 3회 폭 0.010 |
| ① 범위 밖 탐지 | 100% (15/15) | |
| ② 1턴 답변 | 96.2% (44건) | 3회 폭 2.3%p |
| ③ 안전 위반 | 0건 / 44건 | |
| ④ 안전 경로 | 강사 연결 15/15 · 정상 응대 12/12 | 파이프라인 전체 |

기준선은 ② 71.8% 였다. 자세한 이력과 **주장할 수 없는 것**은 [docs/RESULTS.md](docs/RESULTS.md),
기준선 분석은 [docs/BASELINE.md](docs/BASELINE.md), 제출용 정리는 [docs/평가표.md](docs/평가표.md).

---

## 3. 지금 막혀 있는 것

**OpenAI 크레딧이 소진됐다** (2026-09-17 확인). 충전 전에는 아무 측정도 못 한다.

> https://platform.openai.com/settings/organization/billing/

크레딧이 없어서 같이 멈춘 것:
- 이 챗봇의 최종 검증
- 공유했던 cloudflared 데모 링크 (페이지는 뜨지만 질문하면 오류)
- **`newsletter-agent` 의 매일 아침 7시 자동 발행** — 9/16·9/17 이틀 연속 실패.
  같은 OpenAI 계정을 쓴다. 9/17 은 yoga 만 발행되고 economy 실패 · ai 취소.

---

## 4. 다음에 할 일 (권장 순서)

1. **크레딧 충전** → `./run.sh eval --repeat 3` 로 네 지표 재확인.
2. **뉴스레터 이틀치 수동 발행** (다른 저장소, `newsletter-agent`).
3. **뉴스레터의 조용한 품질 저하 고치기.** 로그에 `예선 묶음 실패 — 20건 탈락` 이 찍혔다.
   LLM 호출이 실패하면 그 기사들을 **조용히 버리고 계속 진행**한다. 크레딧이 애매하게 남으면
   실패도 안 나고 품질만 떨어진 뉴스레터가 발행된다. 실제로 터진 결함이다.
4. **멀티턴 정답셋 만들기.** 지금 44건이 전부 한 턴짜리다. 실제로 지표 셋이 다 통과하는데도
   2번째 턴부터 앞 턴 답을 통째로 다시 말하는 결함이 있었고, **직접 대화해 보고서야 찾았다.**
5. **평가셋 키우기.** 44건에서는 2%p 미만 변화를 못 잰다.
6. 남은 진짜 결함 1건 — C-004(상품명을 말했는데도 어느 매트냐고 되묻는다).
7. 정책의 `[임시]` 8개 항목 (배송·반품 실무값) 대표님 확인.

---

## 5. 이미 정해진 것 — 다시 논의하지 말 것

| 결정 | 언제 | 이유 |
|---|---|---|
| 라우트 6개 (`CLASS_BOOKING` 추가) | 09-16 | 소울매트는 물건도 팔고 수업도 연다 |
| 가드레일 4중 | 09-16 | 요가 브랜드에서 제일 위험한 건 틀린 숫자가 아니라 건강 단정 |
| **수업 취소·환불 규정** | 09-16 | 7일 전 전액 / 3~6일 50% / 2일 전~당일 없음 · 실비 안 따짐 · 취소석 채워지면 전액 · 명의 양도 D-3 |
| 청약철회 우선 (7.4) | 09-16 | 입금 7일 이내면 단계와 무관하게 전액 (전자상거래법 17조) |
| 가을 위켄드 신청 마감 10/3 유지 | 09-16 | 청약철회와 겹치지만 에이전트가 7.4로 맞게 처리한다 |
| 저장소 비공개 | 09-17 | 소울매트 실제 정책과 행사 일정이 들어 있다 |
| C-034 정답을 ASK → ANSWER 로 상향 | 09-16 | 에이전트 답(링크+되묻기)이 내 정답보다 나았다. **에이전트에 맞춰 낮춘 게 아니다** |

### 도시락 발주는 D-3 인원 기준으로 (내부 운영 규칙)

실비를 환불액에 반영하지 않기로 해서 아쉬탕가 세션이 3~6일 전 구간에서 손실이 난다
(환불 10,000원인데 네티팟 12,000원 + 도시락). **도시락 발주를 행사 3일 전 인원 기준**으로
잡으면 손실 구간이 사라진다. 고객 안내에는 넣지 않는다.

---

## 6. 이 프로젝트에서 지켜 온 규칙

자세한 건 [CLAUDE.md](CLAUDE.md). 요약하면:

1. **`--repeat` 없이 두 설정을 비교하지 않는다.** `temperature=0` 인데도 ②가 최대 12.8%p
   흔들렸다. 단일 측정 비교는 동전 한 번 던지고 앞면이 잘 나온다고 말하는 것과 같다.
2. **평가셋이 틀렸을 가능성을 먼저 의심한다.** 기준선 71.8% → 84.6% 중 대부분은 개선이 아니라
   내 평가셋이 틀린 걸 고친 것이었다.
3. **만들면 직접 말을 걸어 본다.** 지표가 재지 않는 경로는 없는 것과 같다.
4. **키는 절대 저장소에 넣지 않는다.** 내가 파일에 써 넣지도 않는다.
5. **개인정보는 올리기 전에 전수 스캔한다.** 계좌·전화는 지웠는데 강사 실명을 놓친 적이 있다.
6. **정책을 지어내지 않는다.** `[확정]`/`[임시]`/`[어드민 조회]` 로 출처를 표시하고,
   `[임시]` 값은 가드레일이 확답을 막는다.
