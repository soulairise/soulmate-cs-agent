# 소울매트 고객응대 에이전트 — 작업 규칙

새 세션이면 [HANDOFF.md](HANDOFF.md) 를 먼저 읽는다. 어디까지 왔고 무엇이 막혀 있는지 거기 있다.

## 이 프로젝트가 뭔가

요가 브랜드 **소울매트**의 고객 문의를 분류하고(6라우트), 어드민을 조회해 답하고(13도구),
내보내기 전에 점검하는(4중 가드레일) LangGraph 에이전트.
소울매트는 **물건도 팔고 수업도 연다** — 이 한 가지가 설계 전체를 가른다.

```
route → answer → guard → (END | answer 재시도 | escalate)
```

`src/prompts.py` 가 성능에 가장 크게 영향을 준다. 여기를 고쳤으면 반드시 다시 잰다.

## 측정 규율 — 가장 자주 어기게 되는 것

**`--repeat` 없이 두 설정을 비교하지 않는다.**
`temperature=0` 인데도 ②가 같은 설정에서 최대 **12.8%p** 흔들렸다. 폭보다 작은 변화는
무엇을 고쳐도 잴 수 없다. 개선을 주장하려면 반복 측정 폭 밖이어야 한다.

```bash
./run.sh eval --repeat 3
```

`--repeat` 이 잡는 건 **배치 안의 흔들림뿐**이다. ①은 한 배치 안에서 폭 0.000 인데
배치를 달리하면 0.942~0.971 로 움직인다. 소수점 둘째 자리는 믿지 않는다.

**한 번에 한 군데만 고친다.** 두 군데를 같이 고쳤으면 그렇게 적는다.

**숫자가 나빠졌으면 나빠졌다고 적는다.** [docs/RESULTS.md](docs/RESULTS.md) 에는 올라간 것만이
아니라 **순증 없던 것과 노이즈였던 것**도 그대로 적혀 있다. 그 형식을 유지한다.

## 실패를 볼 때 순서

**에이전트를 고치기 전에 평가셋과 채점기를 먼저 의심한다.**
기준선 71.8% → 84.6% 중 대부분은 개선이 아니라 **내 평가셋이 틀린 걸 고친 것**이었다.
매뉴얼에 `[확정]` 으로 적어 놓고 "도구를 안 불렀다"고 실패 처리했고,
`must` 문자열이 띄어쓰기("배송완료" vs "배송 완료")에 부서졌다.

채점기를 고칠 때의 경계:
> **같은 뜻의 다른 표현을 받아들이는 건 고침이고, 틀린 답을 통과시키는 건 부정이다.**

`must` 항목을 리스트로 주면 '하나라도 있으면 통과'다. 표현을 추가할 땐 정답셋의
`_must_why`(그 문항의 요구사항)를 보고 정말 같은 뜻인지 판단한다.
예: "단품 구매 불가"에 "판매하지 않습니다 / 세트로만"은 같은 뜻 → 넣는다.
"10,000원 환불"에 "전액 환불"은 다른 뜻 → 넣지 않는다.

## 지표가 재지 않는 경로는 없는 것과 같다

- ①②③ 은 **정답 라우트를 직접 넣고** 재기 때문에 라우터를 지나지 않는다.
  그래서 건강 문의가 `OTHER` 로 분류되어 답변·가드레일을 둘 다 건너뛰는 결함을 못 잡았다.
  → ④(`--only e2e`)를 만들어 파이프라인 전체를 재게 했다.
- ①②③④ 전부 **한 턴짜리**다. 2번째 턴부터 앞 턴 답을 다시 말하는 결함은 지표가 못 잡았고
  **직접 대화해 보고서야** 찾았다.

**만들었으면 `./run.sh` 로 직접 말을 걸어 본다.** 이건 선택이 아니다.

## 안전 — 이 프로젝트에서 가장 중요한 것

요가 브랜드에서 제일 위험한 답변은 틀린 숫자가 아니다.

> 고객: "허리 디스크가 있는데 아쉬탕가 해도 될까요?"
> 에이전트: "네, 무리 없이 하실 수 있습니다."

이 한 줄이 나가면 소울매트가 책임을 진다. 세 겹으로 막혀 있다 —
매뉴얼 7.2 · `ANSWER_RULES` 7번 · 가드레일 `건강 판단 단정`.
게다가 **분류보다 먼저** `agent.node_route` 가 `HEALTH_ASK` 로 강제 이관한다.
모델 판단에 안전망을 맡기지 않는다.

`guardrail.py` 의 정규식이 둘인 이유:
- `HEALTH_Q` (넓게) — 답변 사후 점검용
- `HEALTH_ASK` (좁게) — 강제 이관용. 넓게 잡으면 "임산부용 매트 파나요?" 같은 평범한
  상품 문의까지 이관되어 에이전트가 쓸모없어진다. 가르는 기준은
  **"이 몸으로 해도 되냐"(의료 판단)** vs **"그런 상품을 파느냐"(상품 문의)**.

한국어 활용에 주의. `되` 로는 "**될**까요"를 못 잡는다. 가장 중요한 문항이 이것 때문에
새어 나간 적이 있다. 정규식을 건드렸으면 `./run.sh eval --only e2e` 로 27건을 다시 잰다.

## 정책은 지어내지 않는다

`data/policy_soulmate.md` 가 고객에게 하는 **실제 약속**이 된다. 출처를 표시한다.

| 표시 | 뜻 |
|---|---|
| `[확정]` | 대표님이 정했거나 법에 있는 값. 바로 답해도 된다 |
| `[임시]` | 업계 통상값으로 자리만 잡은 것. **가드레일이 확답을 막는다** |
| `[어드민 조회]` | 매뉴얼에 값이 없다. 도구 조회 결과만 쓴다 |

`[임시]` 를 `[확정]` 으로 올릴 땐 `src/context.py` 의
`PROVISIONAL_POLICY` → `CONFIRMED_POLICY` 로도 옮긴다. 부록 B 표도 갱신한다.

## 키와 개인정보

- **키는 저장소에 넣지 않는다.** 코드가 `~/.config/harness-lab/env` 에서 읽는다
  (`config.py:_load_key`). 키를 파일에 써 넣는 일은 대표님이 직접 하신다.
- **공개·공유 전에 전수 스캔한다.** 계좌·전화는 지웠는데 **강사 실명을 놓친 적이 있다.**

```bash
./scan.sh
```

- 목업의 계좌·연락처·신청주소·강사명은 전부 **가짜 값**이다. 실제 값으로 되돌리지 않는다.
`scan.sh` 는 계좌·전화·실명·폼주소·오픈채팅·API키·토큰·접속코드값·로컬경로를 한 번에 본다.
**공개하거나 링크를 공유하기 전에 반드시 돌린다.**

- 웹을 남에게 열 때 `SM_WEB_CODE` 를 반드시 건다. 없으면 주소만 아는 사람이 API 크레딧을 쓴다.

## 평가셋 다룰 때

- **`split == "eval"` 문항을 프롬프트 예시로 넣지 않는다.** 예시가 필요하면 `split == "fewshot"`.
- 문항을 추가·수정했으면 `n` 이 바뀌므로 **이전 행과 직접 비교하지 않는다.** RESULTS 에 적는다.
- 정답셋을 고쳤으면 채점기 자기 검증(모범 답안 전건 통과)부터 확인한다.
