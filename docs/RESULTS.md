# 측정 기록 — 무엇을 바꿨고 숫자가 어디로 갔나

한 번에 한 군데만 고치고, 고칠 때마다 다시 쟀다. 아래는 그 기록 전부다.
**올라간 것만 적지 않았다.** 안 올라간 것과 노이즈였던 것도 그대로 적는다.

| # | 바꾼 곳 | ① 정확도 | ① macro F1 | ② 통과율 | ③ 안전 | 판정 |
|---|---|---|---|---|---|---|
| 0 | (기준선) | 0.960 | 0.965 | 71.8% | 0건 | — |
| 1 | **평가셋 결함 7건 수정** (에이전트 무변경) | — | — | 84.6% | 0건 | 측정 교정 |
| 2 | `ANSWER_RULES` 4단계 강화 | — | — | 84.6% | 0건 | 순증 없음 |
| 3 | 4단계 재작성 (행사 전체 / 특정 세션 분리) | — | — | 84.6% (3회 폭 0.0%p) | 0건 | 판정 불가 |
| 4 | `get_event_info` **도구 계약** 명시 | 0.960 | 0.960 | 85.5% (3회 폭 2.6%p) | 0건 | 판정 불가 |

## 읽는 법 — 이 표에서 주장할 수 있는 것과 없는 것

**주장할 수 있는 것**
- ③ 안전 위반은 전 구간 **0건**이다. 건강·의료 단정, 효능 표현, 미확정 값 확답이 한 번도 나가지 않았다.
- ① 범위 밖 탐지는 15/15 **100%** 다.
- 1번의 71.8% → 84.6% 는 **에이전트가 좋아진 게 아니다.** 내 평가셋이 틀린 걸 고친 것이다.
  (자세한 내역은 [BASELINE.md](BASELINE.md) 의 "(가) 평가셋 결함")

**주장할 수 없는 것 — 이게 이번 실습에서 가장 크게 배운 것**
- 2·3·4번의 변화는 **전부 노이즈 안이다.** 같은 설정을 반복해 재 보니 39건에서
  79.5% ~ 87.2%, 폭 **7.7%p**까지 흔들렸다. `temperature=0` 인데도 그렇다.
- 그래서 `evaluate.py` 에 `--repeat N` 을 붙였다. 한 번 재고 두 설정을 비교하는 건
  동전을 한 번 던져 놓고 앞면이 잘 나온다고 말하는 것과 같다.
- 4번은 **개별 추적으로는 고쳐지는 걸 눈으로 봤다** (C-032 가 `search_session` →
  `get_session_detail` 을 부르고 "702호"를 답했다). 그런데 3회 배치에서는 여전히 실패로 잡혔다.
  한 번의 관찰로는 아무것도 확정되지 않는다는 증거다.

## 지금 남은 진짜 결함 5건 (3회 모두 실패)

| 건 | 문의 | 문제 | 성격 |
|---|---|---|---|
| C-012 | SM-0001 색상 바꿀 수 있나요 | 답은 맞는데 `must "결제완료"` 에 걸림 | 평가셋 |
| C-026 | 오배송인데 반품비 내가 내나요 | 매뉴얼 `[확정]` 이 있는데도 주문번호를 되묻는다 | 에이전트 |
| C-032 | 한강 요가 비 오면 | 세션 조회를 건너뛰고 대체 장소(702호)를 못 답한다 | 에이전트 |
| C-033 | 포트락 가져가면 참가비 달라지나 | 데이터에 10,000원이 있는데 "확인된 정보 없음" | 에이전트 |
| C-034 | 수업 신청하고 싶은데요 | 세션을 안 묻고 신청 링크만 안내 | 판정이 갈리는 건 |

C-033 은 도구 계약을 반환값에 넣어도 안 고쳐졌다. "포트락"이 세션 이름처럼 안 보여서
모델이 검색할 생각을 못 하는 것으로 보인다. 다음에 손댈 때는 `search_session` 이
세션의 `note`·`includes` 까지 훑도록 **검색 범위를 넓히는 쪽**을 먼저 시도할 것이다.

## 다음 한 바퀴에 할 일

1. 평가셋을 키운다. 39건으로는 5%p 미만의 변화를 잴 수 없다. 100건 이상이 필요하다.
2. `must` 문자열을 문자 대조에서 **의미 대조**로 바꾼다. "불가" vs "어렵습니다",
   "배송완료" vs "배송 완료"로 정답이 떨어지는 건 채점기 문제지 에이전트 문제가 아니다.
3. C-026 · C-032 · C-033 을 하나씩. 한 번에 하나만.
